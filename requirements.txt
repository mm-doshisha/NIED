spacy==3.8.5
spacy-alignments==0.9.1
spacy-legacy==3.0.12
spacy-loggers==1.0.5
spacy-transformers==1.3.8
srsly==2.5.1
thinc==8.3.6
typing-extensions==4.12.2
tqdm==4.67.1
numpy==2.0.2

# --- GPU Training Requirements for CUDA 12.x ---
# NOTE: The versions for torch and cupy MUST be installed using CUDA 12.x compatible packages.

# PyTorch (Deep Learning Backend)
torch>=2.0.0, <3.0.0

# Adjusted constraints for compatibility with torch 2.x
torchvision>=0.23.0 
torchaudio>=2.6.0

# CuPy (GPU array backend, required by spaCy/Thinc for GPU functionality)
# We recommend using the CUDA-specific package name for successful installation (e.g., cupy-cuda12x).
cupy